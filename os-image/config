# VibeCodePC — pi-gen build configuration
# Run ./build.sh from this directory to produce the OS image.

IMG_NAME="VibeCodePC"
RELEASE=bookworm
DEPLOY_COMPRESSION=xz
COMPRESSION_LEVEL=6

# Device defaults (changed by user on first-boot/wizard)
TARGET_HOSTNAME=vibecodepc
FIRST_USER_NAME=vibecodepc
FIRST_USER_PASS=vibecodepc
ENABLE_SSH=1

LOCALE_DEFAULT=en_US.UTF-8
KEYBOARD_KEYMAP=us
KEYBOARD_LAYOUT="English (US)"
TIMEZONE_DEFAULT=UTC

# Run standard lite stages (0-2) then our custom stage
STAGE_LIST="stage0 stage1 stage2 ${SCRIPT_DIR}/stage-vibecodepc"

# Skip desktop stages
SKIP_IMAGES=""
